# DAF v0.1.0 — core dependencies
pydantic>=2.0.0

# LLM provider — NOT included. Implement LLMClient for your preferred provider.
# See daf/runtime/llm_client.py for the interface.
#
# Examples:
#   pip install anthropic   # if using Anthropic
#   pip install openai      # if using OpenAI-compatible API
#   pip install ollama      # if using Ollama Python client
